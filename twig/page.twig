{% extends "base.twig" %}

{% block main %}
  <div class="container">
    <div class="wrapper page__wrapper">
      <article class="article">
        <h1 class="layout__title">{{ post.post_title }}</h1>
        <div class="article__content">
            {{ post.content }}
        </div>
      </article>
      <div class="article__comments">
        {{ function('comments_template') }}
      </div>
    </div>
  </div>
{% endblock %}
